# 간단 퀴즈
# 1. 변수 만들기
# 변수 a 에 문자열 `456` 을 할당하고, 변수 b 에 정수 `789` 를 할당하세요.
a = 456
b = 789

# 2. 연산해 보기
#  456 + 789 = 1,245
print(a +b)
# a 와 b를 더하려 하였을 때 (+), 어떠한 일이 일어나나요?
# 코드를 추가하여, 수리적 연산을 완료해 봅시다.

# 3. 식별자와 리터럴
# 변수의 이름을 식별자라고 합니다. 
# 아래는 변수를 할당하는 코드인데, 각각 어떠한 문제점이 있을까요? 
# 1_number = 1.0
# print = "print는 출력하는 함수"

# 왜 이렇게 식별자 이름을 설정하면 안될지 생각해 봅시다.
 ##숫자가 제일 먼저 나오면 무슨 오류가 난다고 했던거 같은데.. 자세히는 기억이 나지 않는다.
 
# 4. 연산자
# 변수 a 에 문자열 `456` 을 할당하고, 변수 b 에 정수 `789` 를 할당하세요.
# 변수 c는 " " 공백이 있는 문자열이고, d 는 a에서 b를 나눈 몫입니다.
print('456')
c = " "
d = a / b
# 두 변수를 가지고
# 비교 연산자, 논리연산자를 사용해서
# True를 반환하는 2개 이상의 케이스를 만들어 내세요.
print(a > b)
print(a < b)
print(a == d)
print(a !=d)
print(a !=b)
b = '456'
print(b)
print(type(b))